<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Task Title</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://unpkg.com/lucide@latest"></script>
</head>
<body class="bg-zinc-900 text-white font-sans">
  <div class="w-full min-h-screen flex flex-col items-center justify-start py-12 px-4">
    
    
    <div class="w-full max-w-xl bg-zinc-800 p-8 rounded-2xl shadow-2xl mb-12">
      <h2 class="text-2xl font-bold mb-6 text-purple-400 text-center">📝 Rename Task</h2>
      
      <form action="/edit/<%= fileName %>" method="POST">
        
        <input type="hidden" name="prevtitle" value="<%= fileName %>">

        <label class="block text-sm mb-2 text-zinc-300">Current Title</label>
        <input 
          class="block w-full mb-4 px-5 py-3 bg-zinc-700 text-white rounded-lg outline-none focus:ring-2 focus:ring-purple-500"
          type="text" 
          value="<%= fileName %>" 
          disabled
        >

        <label class="block text-sm mb-2 text-zinc-300">New Title</label>
        <input 
          class="block w-full mb-6 px-5 py-3 bg-zinc-700 text-white rounded-lg outline-none focus:ring-2 focus:ring-purple-500"
          type="text" 
          name="newtitle" 
          placeholder="Enter new title..." 
          required
        >

        <button 
          class="w-full flex items-center justify-center gap-2 bg-purple-600 hover:bg-yellow-700 text-white font-semibold py-3 rounded-lg transition duration-300"
          type="submit"
        >
          <i data-lucide="edit-3" class="w-5 h-5"></i>
          Rename Task
        </button>

        <div class="mt-4 text-center">
          <a href="/" class="text-blue-400 hover:underline text-sm inline-flex items-center gap-1">
            <i data-lucide="arrow-left" class="w-4 h-4"></i> Back to Home
          </a>
        </div>
      </form>
    </div>
  </div>

  <script>
    lucide.createIcons();
  </script>
</body>
</html>
